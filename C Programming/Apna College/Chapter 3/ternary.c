#include<stdio.h>
int main(){

    // int age;
    // printf("Enter your age: ");
    // scanf("%d", &age);

    // age >= 18 ? printf("Adult\n") : printf("Not adult\n");

    // int marks;
    // printf("Enter your marks: ");
    // scanf("%d", &marks);

    // marks >= 80 ? printf("You got A+\n") : printf("Try next time\n");

    double pi;
    printf("Enter the value of pi: ");
    scanf("%lf", &pi);

    pi == 3.1415 ? printf("Right\n") : printf("Wrong\n");

    return 0;
}
